import { HidenForRolesDirectiveDirective } from './hiden-for-roles-directive.directive';

describe('HidenForRolesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HidenForRolesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
